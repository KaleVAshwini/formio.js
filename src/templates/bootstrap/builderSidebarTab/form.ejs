<div id="myTab" ref="builder-sidebar-tabs" role="tablist">
  <div class="container" style="display: flex;">
    <div class="col-sm border-bottom{{ctx.tab == 1 ? ' border-info text-center' : 'border-info text-center'}}" style="border-width: 2px !important; cursor: pointer; padding: 0px;" ref="basicTab">  
        <img alt="a" src="component.svg" style="height: 4px;" />  
    </div>
    <div class="col-sm border-bottom{{ctx.tab == 2 ? ' border-info text-center' : 'border-info text-center'}}" style="border-width: 2px !important; cursor: pointer; padding: 0px;" ref="customTab">
      <img alt="a" src="data.svg" style="height: 4px;" />
    </div>
    <div class="col-sm border-bottom{{ctx.tab == 3 ? ' border-info text-center' : 'border-info text-center'}}" style="border-width: 2px !important; cursor: pointer; padding: 0px;" ref="othersTab">
      <img alt="a" src="layout.svg" style="height: 4px;"  />
    </div>
  </div>
<!-- 

  <li class="nav-item" role="presentation" ref="basicTab">
    <a class="nav-link {{ctx.tab == 1 ? 'border-bottom' : ''}}"  id="home-tab" data-toggle="tab"  role="tab" aria-controls="basic" >Basic</a >
  </li>
  <li class="nav-item" ref="customTab" role="presentation">
    <a  class="nav-link {{ctx.tab == 2 ? 'active' : ''}}" id="profile-tab" data-toggle="tab" role="tab" aria-controls="custom">Custom</a>
    </li>
  <li class="nav-item" ref="othersTab" role="presentation">
    <a  class="nav-link {{ctx.tab == 3 ? 'active' : ''}}" id="contact-tab" data-toggle="tab" role="tab" aria-controls="others" >Others</a>
    </li> -->
  </div>
